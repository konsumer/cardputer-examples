# sound

This is a simple sound example, that records a snippet, and plays it back when you press the BOOT button.

Hold down the BOOT button to turn on a noise-filter.

```sh
pio run --target upload -e sound
```