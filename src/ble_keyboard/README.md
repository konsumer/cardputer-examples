# BLE Keyboard

This is a simple BLE keyboard. It shows how to emulate a HID device. It uses [NimBLE-Arduino](https://github.com/h2zero/NimBLE-Arduino), which generally works a bit better for HID devices.


```sh
pio run --target upload -e ble_keyboard
```