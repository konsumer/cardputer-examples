This is for [8encoder](https://docs.m5stack.com/en/unit/8Encoder).

The [arduino library](https://github.com/m5stack/M5Unit-8Encoder) was not working for me for some reason (boot-looping) so I made this demo (and simple driver library.)

```sh
pio run --target upload -e 8encoder
```