# GPS

Basic test of GPS on Lora Cap 1262](https://docs.m5stack.com/en/cap/Cap_LoRa-1262). I had to go outside to get a fix, but it fixes faster after that, even indoors (it caches satellites.)


```sh
pio run --target upload -e gps
```