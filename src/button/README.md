# Button

Shows the state of BOOT button (on top, not RESET.)

This is the sma ebutton you hold when powering-on to force it into BOOT-mode.


```sh
pio run --target upload --target monitor -e button
```